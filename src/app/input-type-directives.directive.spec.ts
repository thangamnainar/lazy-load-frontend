import { InputTypeDirectivesDirective } from './input-type-directives.directive';

describe('InputTypeDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTypeDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
